# bronze-revision

Roblox Studio script export for `Pokemon Brick bronze | Pokemon Revision`.

- `scripts/` contains the exported Lua sources grouped by top-level Roblox service.
- `manifest.json` maps each saved file back to its Roblox instance path, class, disabled state, source length, and SHA-256 hash.
- `README.txt` contains the original export summary.

Export summary:

- Scripts reported: 635
- Scripts saved: 635
- Read errors: 0
